# FluxVault
FluxVault is a local-first password manager that stores credentials in an encrypted vault secured by a master password.

The app uses the Web Crypto API (AES-GCM + PBKDF2) to ensure your data never leaves your device.
